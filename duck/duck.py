#!/usr/bin/env python3


if __name__ == '__main__':
    # You should write your functions so that they don't care about what their input types are.  This is true for most
    # built in functions, such as abs.  This is known as duck typing.
    print(abs(1), abs(1.2), abs(1 + 2j))

    # However, it doesn't work for some types.  Uncomment this to see.
    #print(abs('abc'))

